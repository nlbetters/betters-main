# constants.py
# This module contains constants used for Morse code encoding and decoding.
# This will be used for the audio generation and decoding parameters.

DEFAULT_WPM = 18               # words per minute
DEFAULT_FREQUENCY = 650        # in Hz
DEFAULT_SAMPLE_RATE = 44100    # in Hz
DEFAULT_AMPLITUDE = 0.3
DEFAULT_THRESHOLD = 0.02       # for detection
DEFAULT_SQUELCH_MS = 20        # in milliseconds